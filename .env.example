# EchoEtcher Configuration File
# Copy this file to .env and update with your actual paths and settings

# ============================================================================
# Required Settings
# ============================================================================

# Path to the folder to watch for new audio files (e.g., iCloud folder)
WATCH_FOLDER=/path/to/your/icloud/folder

# Path to your Obsidian vault root directory
OBSIDIAN_VAULT_PATH=/path/to/your/obsidian/vault

# Ollama model to use for processing transcriptions
# Make sure you've pulled this model: ollama pull mistral
OLLAMA_MODEL=mistral

# ============================================================================
# Optional Settings
# ============================================================================

# Folder name within the vault where notes will be created
# Default: "notes"
NOTES_FOLDER=Daily Notes

# Ollama API URL
# Default: http://localhost:11434/api/generate
OLLAMA_API_URL=http://localhost:11434/api/generate

# Ollama temperature (0.0-1.0, lower = more deterministic)
# Default: 0.3
OLLAMA_TEMPERATURE=0.3

# Ollama API timeout in seconds
# Default: 120
OLLAMA_TIMEOUT=120

# Whisper model size (tiny, base, small, medium, large, large-v2, large-v3)
# Smaller models are faster but less accurate
# Default: medium
WHISPER_MODEL_SIZE=medium

# Path to allowed tags file (relative to vault root or absolute path)
# Default: allowed_tags.md (in vault root)
ALLOWED_TAGS_FILE=allowed_tags.md
